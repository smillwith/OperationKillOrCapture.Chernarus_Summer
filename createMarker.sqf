dingus_fnc_AddTaskMarker = {
  params ["_pos", "_text"];

  //Create it
  //createMarker

  //Set type

  //Set color

};

/*
1. Place base markers that are invisible
2. When each 'info' task is complete, call the script that will swap the marker to a visible version (uses question mark)
3. When the 'visit' task is complete, call the script to swap it to the complete version (green circle).
*/
